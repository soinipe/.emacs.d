Helper scripts:
- emacs scripts
- a few shell scripts

Install:
- `$ cd ~`
- `$ git clone https://github.com/soinipe/.emacs.d.git .emacs.d`
- `$ cp .emacs.d/.bash_aliases .`
- `$ mkdir bin`
- `$ cp .emacs.d/clear~s  bin`
